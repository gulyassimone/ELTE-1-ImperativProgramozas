#include <stdio.h>
#include <stdlib.h>

int main()
{
    int r;
    float pi=3.1415;
    scanf("%d", &r);
    printf("Kor kerulete: %.2f Kor terulete: %.2f ", (float)2*r*pi,(float)r*r*pi);
    return 0;
}
